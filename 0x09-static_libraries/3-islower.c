#include "main.h"
/**
 * _islower - function checks for lowercase character
 * @c: the character to check
 * Return: 0 if lowwercase and 0 if not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
