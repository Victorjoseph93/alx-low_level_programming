#include "main.h"
/**
 * _isdigit - this function will check for a digit
 * @c: integer to be checked
 * Return: will return 1 if digit and 0 if not
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
